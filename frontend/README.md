# Frontend Mastercota (Next.js)

UI web. L’API vit dans `../backend`.

```bash
cp .env.example .env.local
npm install
npm run dev
```

`BACKEND_URL` (défaut `http://127.0.0.1:4000`) est utilisé pour le SSR et les rewrites `/api/*`.
